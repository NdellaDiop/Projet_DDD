/** Logo institutionnel (fichier dans `public/`, servi à la racine). */
export const BRAND_LOGO_PATH = "/logo-dem-dikk.png";

/**
 * Photo de fond du bandeau d'accueil (bus Dakar Dem Dikk).
 * Déposer le fichier dans `dddfront/public/` (ex. hero-bus.jpg).
 */
export const HERO_BACKGROUND_IMAGE = "/hero-bus.jpg";

/** Logo horizontal : hauteur visuelle (le fichier a souvent des marges transparentes). */
/** Header : pas de marge basse (alignement avec le libellé à droite du logo). */
export const BRAND_LOGO_CLASS_HEADER = "h-10 w-auto shrink-0 sm:h-12 md:h-14";
export const BRAND_LOGO_CLASS_FOOTER = "mb-1 h-14 w-auto shrink-0 sm:h-[3.75rem] md:h-16";
export const BRAND_LOGO_CLASS_DASHBOARD = "h-12 w-auto shrink-0 sm:h-14 md:h-[3.75rem]";
